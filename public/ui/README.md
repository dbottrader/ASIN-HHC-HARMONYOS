# UI Archive Index

This directory is for ASIN-HHC / CP8 user interfaces, visual tools, one-file HTML deliverables, and runnable symbolic systems.

---

## UI Philosophy

The project favors:

- UI-first exploration;
- one-file HTML where practical;
- copy-ready artifacts;
- visual learning;
- symbolic interaction;
- local-first demos before production claims.

---

## Known UI Artifacts

### Cycle 010 Workflow

Status: pending migration  
Evidence class: `LOCAL` once the HTML/source file is committed  
Known concepts:

- rooms: Vault, Resonance, Workshop, Bridge, Expansion, Archive;
- frequencies: 428 Hz, 528 Hz, 963 Hz;
- header: `11111101001.1010.11110`.

### DIGMA COUSA 28-Glyph OS

Status: pending migration  
Evidence class: `LOCAL` once committed  
Purpose: symbolic glyph operating interface.

### ASIN Handshake Image Engine

Status: pending migration  
Evidence class: `LOCAL` or `REPRO` depending on whether build instructions and dependencies are included.

### Harmonic Portal

Status: pending migration  
Evidence class: `LOCAL` once committed.

### DNA Resonator

Status: pending migration  
Evidence class: `LOCAL` once committed.

### Light Chimes

Status: pending migration  
Evidence class: `LOCAL` once committed.

### Crop Glyph Harmonizer

Status: pending migration  
Evidence class: `LOCAL` once committed.

---

## Required UI Metadata

Each UI should include a small header comment or companion README:

```text
title:
version:
date:
author_or_agent:
evidence_class:
inputs:
outputs:
dependencies:
claim_boundary:
```

---

## Promotion Path

```text
HTML mockup -> local runnable UI -> reviewed UI -> reproducible build -> deployed production app
```
